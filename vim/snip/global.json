{
    "dateS": {
        "prefix": ["dateS"],
        "body": ["${CURRENT_YEAR}/${CURRENT_MONTH}/${CURRENT_DATE}"],
        "description": "date splited slash"
    },
    "date-jp": {
        "prefix": ["datejp"],
        "body": ["${CURRENT_YEAR}年${CURRENT_MONTH}月${CURRENT_DATE}日"],
        "description": "date with japanese"
    }
}
